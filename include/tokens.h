#pragma once /* Include this header only once */

/* Count words in a line */
int total_words_in_row(const char *line);

/* Get specific word by number from a line */
char *get_word_number(const char *line, int id);
